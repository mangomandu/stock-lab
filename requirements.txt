# 자양동 Stock Lab — Python 의존성
# 사용: python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt
# Mac 이전 시 활용 (Docker 대신)

yfinance>=0.2.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
lightgbm>=4.0.0
xgboost>=2.0.0
